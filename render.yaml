services:
  - type: web
    name: finance-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python tg_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false